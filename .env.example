# Admin panel password
ADMIN_PASSWORD=changeme

# Postgres connection (Heroku sets DATABASE_URL automatically when you add Postgres addon)
DATABASE_URL=postgres://localhost:5432/sp_whatsapp

# Port (Heroku sets this automatically)
PORT=3000
